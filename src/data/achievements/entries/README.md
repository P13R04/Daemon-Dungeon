# Achievement Entries

Place one JSON file per achievement in this folder.

Example filename: `achievement_process_terminator.json`

The Achievement Editor (`content_tools/achievement-editor.html`) can save files here directly and can also generate a backward-compatible `achievements.json` aggregate.
