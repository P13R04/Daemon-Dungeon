# Bonus Entries

Place one JSON file per bonus in this folder.

Example filename: `bonus_damage_overflow.json`

The Bonus Editor (`content_tools/bonus-editor.html`) can save files here directly and can also generate a backward-compatible `items.json` aggregate.
